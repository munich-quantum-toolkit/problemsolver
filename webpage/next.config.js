/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/problemsolver",
  assetPrefix: "/problemsolver/",
  images: { unoptimized: true },
};

module.exports = nextConfig;
